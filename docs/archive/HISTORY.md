# История разработки проекта (Июнь 2026)

Этот файл содержит архивные записи о выполненных задачах и этапах разработки, перенесённые из директории `.remember`.

---

## Неделя от 2026-06-15
*   Создан тестовый каркас для аудита совместимости с C# референсом (сравнение 34 логических блоков, 280+ расхождений).
*   Реализована пушка (`Gun`) с графическим интерфейсом и логикой нанесения урона.
*   Совместимость клиента и сервера доведена до уровня 26 из 32 блоков.
*   Исправлено 6 критических ошибок на клиенте (утечки при реконнекте, зависание программатора, проблемы в `TCPConnection`).
*   Развёрнуто окружение разработчика (тема Catppuccin, Nerd Font, хуки pre-commit).

---

## 2026-06-22
*   Завершён аудит паритета с C# (34 блока, 280+ расхождений).
*   Внедрена шина команд P2 (очередь жизненного цикла `LifeCmd`) после отката P1 из-за дублирования логики очередей.
*   Полностью готов функционал аукциона (end-to-end).
*   Найдено узкое место в физике песка (`sand_physics`) на 6000 игроков (пики до 128 мс).
*   Исправлена критическая уязвимость безопасности в операциях с кланами (отсутствие проверок `clan_id`).
*   Создан каркас GUI (`horb.rs`) с мини-картой на холсте и инструментами администратора.
*   Исправлено 10+ багов совместимости, успешно пройдено 158+ тестов.

---

## 2026-06-23
*   Завершён графический интерфейс программатора (меню `Pope`, предмет упаковки клана №4 открывает здание).
*   Проведён аудит безопасности в 15 подсистемах:
    *   Исправлено 3 IDOR-уязвимости в кланах (согласие на инвайт, продвижение, исключение).
    *   Пароли переведены из открытого текста в SHA-256.
    *   Устранено 5 эксплойтов дублирования ресурсов/уязвимостей состояния (TOCTOU/переполнение).
*   Достигнут побайтово идентичный паритет генерации мира между C# и Rust (исправлено 5 багов в `generator.rs` и `sectors_gen.rs`).
*   Архитектурный аудит выявил 3 критические ошибки (зависание в цикле тиков, устаревание позиций игроков, модель владения ячейками).
*   Проведена миграция конфигураций в `configs/`, сделано 18+ коммитов.

---

## 2026-06-24
*   Завершено укрепление ядра: исправлено 3+ критических бага (физика в `sand.rs`, рассинхронизация зданий, падения чата).
*   Объединены рабочие процессы CI/CD на GitHub Actions (время сборки сокращено с 5–6 мин до 2.5 мин за счёт параллелизации).
*   Исправлены ошибки в тестах программатора (синхронизация движения, окна, диалоги).
*   Пройден аудит безопасности прокси и песка.
*   Развёрнут деплой с нулевым временем простоя, зафиксировано 8 версий GitHub Actions, тесты проходят на 100% (183 теста).
