//! Смещение по сетке для направления игрока (0–3), как в клиенте.

#[must_use]
pub const fn dir_offset(dir: i32) -> (i32, i32) {
    match dir {
        0 => (0, 1),  // down
        1 => (-1, 0), // left
        2 => (0, -1), // up
        3 => (1, 0),  // right
        _ => (0, 0),
    }
}
