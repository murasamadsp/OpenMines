[alias]
server = "run --bin openmines-server --"
regen = "run --release --bin openmines-server -- --regen"
doctor = "run --bin openmines-server -- --doctor"
check-server = "check -p openmines-server"
test-fast = "nextest run --all-targets --all-features"
